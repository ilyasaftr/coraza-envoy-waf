apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: coraza-ext-proc-from-apps
  namespace: gateway-system
spec:
  from:
    - group: gateway.envoyproxy.io
      kind: EnvoyExtensionPolicy
      namespace: demo
  to:
    - group: ""
      kind: Service
      name: coraza-ext-proc

