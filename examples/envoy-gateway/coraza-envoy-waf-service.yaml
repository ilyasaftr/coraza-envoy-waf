apiVersion: v1
kind: Service
metadata:
  name: coraza-ext-proc
  namespace: gateway-system
spec:
  selector:
    app: coraza-ext-proc
  ports:
    - name: grpc
      port: 9002
      targetPort: grpc
    - name: metrics
      port: 9090
      targetPort: metrics

